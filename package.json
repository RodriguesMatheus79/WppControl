{
  "name": "new-project",
  "version": "1.0.0",
  "description": "Local development server for the project",
  "scripts": {
    "dev": "http-server . -p 3000 -o --cors",
    "start": "http-server . -p 3000 -o --cors"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
